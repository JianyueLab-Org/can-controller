# 本地开发。三个都不是密钥 —— 它们是主机名，容器里也是明文环境变量。
#
# PUBLIC_ORIGIN 本地一定要设：写操作会拿浏览器发来的 Origin 和它比对，不设就是
# 拿线上地址去比对 http://localhost:4326，于是「开一条预约」和「退出登录」在本
# 地永远 403。

# can-api。数据全部来自它；不指过来就是读线上的真实数据。
CAN_API_ORIGIN=https://api.ceruleanavi.net

# can-web。登录页、侧栏那一排跨站链接，以及 ATIS maker URL 的主机名。
# 最后一条不要改成本站 —— 那条 URL 已经粘进管制员各自机器上的 EuroScope 了。
CAN_WEB_ORIGIN=https://ceruleanavi.net

# 本站自己的对外地址。
PUBLIC_ORIGIN=http://localhost:4326
